import { CovidState } from './types';

const state: CovidState = {
    covidPage: {
        covidIndivisualData: [],
        covidIndivisualCount: 0,
        covidGroupData: [],
        covidGroupCount: 0,
    },
};
export default state;
